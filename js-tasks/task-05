//https://www.codewars.com/kata/keep-hydrated-1/train/javascript

function litres(time) {
  return Math.floor(time * 0.5);
}

function litres(time) {
  const roundedAmount = Math.floor(0.5 * time)
  return roundedAmount;
}
